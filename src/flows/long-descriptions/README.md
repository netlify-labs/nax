# Long Descriptions Flow

Dashboard-only layout stress workflow with intentionally long step descriptions.

Use it to verify that React Flow cards keep a clear vertical gap when custom node content wraps onto multiple lines.

## Run

```bash
nax dashboard long-descriptions
```
